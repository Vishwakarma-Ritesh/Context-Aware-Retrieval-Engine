# Vector Metadata Schema

Indexed chunks preserve `chunk_id`, `document_id`, `title`, `source`, and topic-level metadata so retrieval results can be traced back to the source document and migrated to a persistent store later.
