# Migrations

The current assessment keeps vector metadata in-process, but this directory is reserved for future persistence migrations when moving chunk metadata into PostgreSQL or DynamoDB alongside a managed vector backend.
